import React, {useEffect} from 'react';

const Music = (props) => {
    useEffect(() => props.handleDrawerClose(), [])
    return (
        <div>
            <h2>in developing</h2>
        </div>
    );
};

export default Music;
