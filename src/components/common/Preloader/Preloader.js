import React from "react";
import SimpleBackdrop from './SimpleBackdrop';

let Preloader = (props) => {
    return (
        <div>
            <SimpleBackdrop/>
        </div>
    )
};

export default Preloader;